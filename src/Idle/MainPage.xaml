<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:Idle.Logic.ViewModels;assembly=Idle.Logic"
             x:Class="Idle.MainPage"
             x:DataType="{x:Type vm:MainPageViewModel}"
             BackgroundColor="#F0F0F3" >

    <StackLayout Margin="36">

        <Button Text="Languages" TextColor="White" Command="{Binding NavigateToLanguagesPageCommand}" BackgroundColor="{StaticResource Purple}" CornerRadius="12"/>
        <Button Text="Shop" TextColor="White" Command="{Binding NavigateToShopPageCommand}"  BackgroundColor="{StaticResource Purple}" CornerRadius="12"/>

    </StackLayout>

</ContentPage>
